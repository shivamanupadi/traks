{
  "name": "@traks/collect",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "doppler secrets download --no-file --format=env -p traks-collect -c dev > .dev.vars.doppler && touch .dev.vars && wrangler dev --remote --port 5010 --env-file .dev.vars.doppler --env-file .dev.vars",
    "build": "tsc --noEmit",
    "deploy:dev": "wrangler deploy",
    "worker:prod:deploy": "wrangler deploy --env production",
    "secrets:prod:sync": "doppler secrets --json -p traks-collect -c prd | jq -c 'with_entries(select(.key | test(\"^DOPPLER_\") | not) | .value = .value.computed)' | wrangler secret bulk --env production",
    "release:prod": "yarn secrets:prod:sync && yarn worker:prod:deploy",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts --fix",
    "format": "prettier --write src",
    "clean": "rimraf .turbo .wrangler .dev.vars.doppler"
  },
  "dependencies": {
    "@traks/shared": "*",
    "hono": "^4.6.16",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260219.0",
    "@traks/eslint-config": "*",
    "eslint": "^8.57.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.107.1"
  },
  "engines": {
    "node": ">=20"
  }
}
